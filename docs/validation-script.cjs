// Local, isolated browser validation. Uses only synthetic data and mocked AI replies.
const fs=require('fs'),path=require('path'),crypto=require('crypto');
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');
const out=path.join(__dirname,'validation-screenshots');fs.mkdirSync(out,{recursive:true});
const url=process.env.LEARNFLOW_TEST_URL||'http://127.0.0.1:4173/';
const results={date:new Date().toISOString(),url,syntheticData:true,mockedModel:true,tests:[],errors:[]};
function check(id,ok,detail){results.tests.push({id,status:ok?'PASS':'FAIL',detail});console.log(id,ok?'PASS':'FAIL',JSON.stringify(detail));}
function seed(){return {version:1,profile:{goal:'QA 测试：复盘一个知识点',minutes:10,guide:'strong',style:'warm',encourage:'strong',memory:true},adopted:['roots','plain','feynman'],favorites:['roots'],ratings:{roots:4},usage:{},custom:[],memories:[{id:'qa-memory',title:'我的表达偏好',content:'QA_MEMORY_MARKER\n输出偏好：自然段落。引导：直接讲清楚。',tags:['自然段落','直接讲清楚'],scene:'应试学习',sceneId:'exam',created:'2026-09-06T00:00:00.000Z'}],sessions:[],blocks:0,tasks:[{id:'qa-task',title:'完成一次证据复盘',owner:'测试角色',evidence:'可检查的独立作答',status:'todo'}],scene:'exam',onboarded:true};}
async function reset(page,data=seed()){await page.goto(url);await page.evaluate(s=>localStorage.setItem('learnflow.v1',JSON.stringify(s)),data);await page.reload();}
async function pick(page,selector,payload){const pending=page.waitForEvent('filechooser');await page.locator(selector).click();const chooser=await pending;await chooser.setFiles({name:'qa.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(payload))});}
async function send(page,text){await page.locator('#chat-input').fill(text);await page.locator('#chat-form button[type="submit"]').click();}
async function storage(page){return page.evaluate(()=>JSON.parse(localStorage.getItem('learnflow.v1')));}
(async()=>{
 const browser=await chromium.launch({headless:true,executablePath:process.env.CHROME_PATH||'C:/Program Files/Google/Chrome/Application/chrome.exe'});results.browser=browser.version();
 const context=await browser.newContext({viewport:{width:1440,height:1000},acceptDownloads:true});const page=await context.newPage();page.on('pageerror',e=>results.errors.push(e.message));await reset(page);
 let captured;await page.route('**/api/chat',async route=>{captured=route.request().postDataJSON();await route.fulfill({json:{reply:'QA_REPLY_MARKER：这是一条测试模拟回复。',provider:'QA stub',usage:{total_tokens:101}}});});
 await page.locator('[data-scene="exam"]').click();await page.locator('#chat-mode').selectOption('api');await send(page,'QA 独立测试消息');await page.waitForFunction(()=>document.querySelector('#messages')?.innerText.includes('QA_REPLY_MARKER'));
 check('01-confirmed-memory-context',JSON.stringify(captured.memories).includes('QA_MEMORY_MARKER'),{memories:captured.memories?.length,requestKeys:Object.keys(captured)});
 await page.reload();check('02-session-restoration',(await page.locator('#messages').innerText()).includes('QA_REPLY_MARKER'),{savedSessions:(await storage(page)).sessions.length});await page.screenshot({path:path.join(out,'desktop-restored-session.png'),fullPage:true});
 await page.goto(url+'#settings');await page.locator('[name="memory"]').uncheck();await page.locator('#settings-form button[type="submit"]').click();await page.goto(url+'#session');await page.locator('#chat-mode').selectOption('api');await send(page,'QA 关闭记忆请求');await page.waitForTimeout(120);check('03-memory-off',Array.isArray(captured.memories)&&captured.memories.length===0,{memories:captured.memories});
 await page.unroute('**/api/chat');await reset(page);
 await page.locator('[data-scene="exam"]').click();await page.locator('#chat-mode').selectOption('api');let release,started;const gate=new Promise(r=>release=r),called=new Promise(r=>started=r);let raceRequest;
 await page.route('**/api/chat',async route=>{raceRequest=route.request().postDataJSON();started();await gate;await route.fulfill({json:{reply:'QA_RACE_REPLY',provider:'QA stub',usage:{total_tokens:100}}});});
 await send(page,'QA 正在请求');await called;await page.locator('a[href="#home"]').first().click();await page.locator('[data-scene="self"]').click();const blocked=(await page.locator('#toast').innerText()).includes('处理中');release();await page.waitForFunction(()=>JSON.parse(localStorage.getItem('learnflow.v1')).usage.roots?.actual===50);let saved=await storage(page);
 check('04-request-race',blocked&&saved.usage.roots.actual===50&&saved.usage.plain.actual===50&&!saved.usage.feynman&&saved.sessions[0].scene==='exam'&&saved.sessions[0].messages.some(x=>x.content==='QA_RACE_REPLY'),{blocked,requestCards:raceRequest.strategies.map(c=>c.id),usage:saved.usage,savedScene:saved.sessions[0]?.scene});
 await page.unroute('**/api/chat');await reset(page);await page.locator('[data-scene="self"]').click();await page.goto(url+'#memory');await page.locator('[data-memory-edit="qa-memory"]').click();const chosen={format:await page.locator('[name="format"]').inputValue(),guidance:await page.locator('[name="guidance"]').inputValue()};await page.locator('#memory-form button[type="submit"]').click();await page.waitForTimeout(30);let memory=(await storage(page)).memories[0];check('05-memory-edit-preserves-fields',chosen.format==='自然段落'&&chosen.guidance==='直接讲清楚'&&memory.sceneId==='exam'&&(memory.content.match(/输出偏好/g)||[]).length===1,{chosen,scene:memory.sceneId,content:memory.content});await page.screenshot({path:path.join(out,'memory-preserved.png'),fullPage:true});
 await reset(page);await page.locator('[data-scene="self"]').click();await page.locator('[data-prompt="我想建立一周的自学路线"]').click();const plan=await page.locator('#messages').innerText();check('06-ten-minute-plan',plan.includes('前 2 分钟')&&plan.includes('中间 6 分钟')&&plan.includes('最后 2 分钟'),{allocation:'2 + 6 + 2'});
 await reset(page);await page.goto(url+'#settings');const downloadWait=page.waitForEvent('download');await page.locator('[data-action="export-all"]').click();const download=await downloadWait;const stream=await download.createReadStream();let chunks=[];for await(const chunk of stream)chunks.push(chunk);const backup=JSON.parse(Buffer.concat(chunks).toString());await page.locator('[data-action="clear-data"]').click();await page.locator('[data-action="confirm-clear"]').click();await pick(page,'[data-action="restore-backup"]',backup);await page.locator('#confirm-restore').click();saved=await storage(page);check('07-full-backup-roundtrip',saved.profile.goal===backup.profile.goal&&saved.memories[0].content===backup.memories[0].content&&saved.tasks[0].title===backup.tasks[0].title&&saved.ratings.roots===4,{memories:saved.memories.length,tasks:saved.tasks.length,rating:saved.ratings.roots});const before=JSON.stringify(saved);await pick(page,'[data-action="restore-backup"]',{version:99});await page.waitForTimeout(50);check('08-invalid-backup-no-write',JSON.stringify(await storage(page))===before,{toast:await page.locator('#toast').innerText()});
 await reset(page);await page.goto(url+'#market');const html='<img src=x onerror="window.__QA_XSS=1">';await pick(page,'[data-action="import-card"]',{title:html,description:html,instructions:html,scene:'self',tags:[html]});await page.waitForFunction(()=>JSON.parse(localStorage.getItem('learnflow.v1')).custom.length===1);const custom=(await storage(page)).custom[0];await page.locator(`[data-detail="${custom.id}"]`).first().click();await page.locator('details').click();await page.waitForTimeout(60);check('09-card-html-escaped',await page.evaluate(()=>!window.__QA_XSS&&document.querySelector('#modal-content').innerText.includes('<img src=x')&&document.querySelectorAll('#modal-content img').length===0),{payload:'img/onerror shown as text',autoEnabled:(await storage(page)).adopted.includes(custom.id)});await page.screenshot({path:path.join(out,'imported-html-as-text.png'),fullPage:true});await page.locator('[data-action="close"]').first().click();
 // The full backup path accepts IDs too; exercise its separate attribute boundary.
 await reset(page);await page.goto(url+'#settings');const id='custom-\"><img src=x onerror="window.__QA_BACKUP_XSS=1">';const evil=seed();evil.custom=[{id,title:'QA backup id',description:'synthetic',instructions:'test',scene:'self',tags:[]}];await pick(page,'[data-action="restore-backup"]',evil);await page.waitForTimeout(80);const rejected=(await page.locator('#confirm-restore').count())===0;if(!rejected){await page.locator('#confirm-restore').click();await page.goto(url+'#market');await page.locator('button').filter({has:page.locator('h3',{hasText:'QA backup id'})}).click();await page.waitForTimeout(100);}check('10-backup-id-attribute-escaped',await page.evaluate(()=>!window.__QA_BACKUP_XSS),{rejected,injected:await page.evaluate(()=>Boolean(window.__QA_BACKUP_XSS)),toast:await page.locator('#toast').innerText()});if(!rejected)await page.locator('[data-action="close"]').first().click();
 await reset(page);await page.locator('[data-scene="self"]').click();await page.locator('[data-prompt="我想建立一周的自学路线"]').click();await page.locator('[data-action="complete-block"]').click();await page.getByRole('button',{name:'这次不保存'}).click();await page.reload();await page.locator('[data-action="complete-block"]').click();saved=await storage(page);check('11-complete-block-reload-idempotent',saved.blocks===1,{blocks:saved.blocks});await page.locator('[data-action="close"]').first().click();
 const mobile=await context.newPage({viewport:{width:390,height:844}});await mobile.setViewportSize({width:390,height:844});mobile.on('pageerror',e=>results.errors.push(e.message));await reset(mobile,seed());await mobile.screenshot({path:path.join(out,'mobile-home-390.png'),fullPage:false});await mobile.getByRole('button',{name:'展开导航'}).click();await mobile.waitForTimeout(250);const opened=await mobile.locator('.sidebar').evaluate(el=>el.classList.contains('open')&&el.getBoundingClientRect().left>=-1);await mobile.screenshot({path:path.join(out,'mobile-navigation-390.png'),fullPage:false});await mobile.locator('#navigation a[href="#market"]').click();await mobile.waitForTimeout(250);const closed=await mobile.locator('.sidebar').evaluate(el=>!el.classList.contains('open')&&el.getBoundingClientRect().right<=1);const overflow=await mobile.evaluate(()=>document.documentElement.scrollWidth>window.innerWidth+1);check('12-mobile-navigation',opened&&closed&&mobile.url().endsWith('#market')&&!overflow,{opened,closed,overflow,viewport:390});await mobile.screenshot({path:path.join(out,'mobile-market-390.png'),fullPage:false});
 await browser.close();results.hashes={};for(const file of ['public/app.js','public/data.js','public/styles.css','server/server.mjs'])results.hashes[file]=crypto.createHash('sha256').update(fs.readFileSync(path.join(__dirname,'..',file))).digest('hex');fs.writeFileSync(path.join(__dirname,'validation-results.json'),JSON.stringify(results,null,2));console.log('REPORT',results.tests.length,'tests',results.tests.filter(t=>t.status==='FAIL').length,'failures');
})().catch(e=>{console.error(e);results.fatal=String(e);fs.writeFileSync(path.join(__dirname,'validation-results.json'),JSON.stringify(results,null,2));process.exitCode=1});
